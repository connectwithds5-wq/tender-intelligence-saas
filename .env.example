# Server
PORT=3000

# Supabase (required for production persistence + auth)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Generic tender feed adapter. The endpoint must return either an array
# or { "tenders": [...] } of tender-like JSON objects.
TENDER_FEED_URL=
